namespace Assignment1
{
    /// <summary>
    /// A list of some Bird subtypes. Actually, all of them, at least currently.
    /// </summary>
    public enum Birds
    {
        Eagle,
        Penguin,
    }
}